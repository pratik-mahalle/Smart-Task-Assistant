# API Keys - You need at least ONE of these
# The app will use Anthropic if available, otherwise falls back to OpenAI

# Option 1: Anthropic API Key (recommended for A2UI demo)
# Get your key from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Option 2: OpenAI API Key (fallback)
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Optional: Set to 'development' or 'production'
NODE_ENV=development
